export enum InputType {
  button = 'button',
  checkbox = 'checkbox',
  color = 'color',
  date = 'date',
  datetimeLocal = 'datetime-local',
  email = 'email',
  file = 'file',
  hidden = 'hidden',
  image = 'image',
  month = 'month',
  number = 'number',
  password = 'password',
  radio = 'radio',
  range = 'range',
  reset = 'reset',
  search = 'search',
  submit = 'submit',
  tel = 'tel',
  text = 'text',
  time = 'time',
  url = 'url',
  week = 'week',
}

export enum InputMode {
  none = 'none',
  text = 'text',
  decimal = 'decimal',
  numeric = 'numeric',
  tel = 'tel',
  search = 'search',
  email = 'email',
  url = 'url',
}

export enum InputState {
  default = 'default',
  error = 'error',
  success = 'success',
}
